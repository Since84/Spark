<section class="our-mission contact">
	<h1 style="background-image: url({{ post.thumbnail.src }})">{{ post.post_title }}</h1>
	<div class="container">{{ post.content }}</div>
</section>
<section class="contact">
	{{ form }}
</section>

