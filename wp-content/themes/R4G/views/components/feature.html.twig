<section class="home-feature {{ spark_class }}" >
	<div class="container" style="background-image: url({{ image.url }});">
		<hgroup>
			<h3>{{ sub_heading }}</h3>
			<h2>{{ heading }}</h2>
		{% if link != "null" %}
			<a href="{{ link }}">
		{% endif %}
				<span class="link-peek"></span>
		{% if link != "null" %}
			</a>
		{% endif %}
		</hgroup>
	</div>	
</section>